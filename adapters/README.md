# Adapters

Transport adapters are implemented under `lib/agent_platform/adapters/`. This directory reserves the product-level extension surface and documents that adapters select Gateway capabilities; they do not parse or mutate Vault Markdown directly.
