# Templates

The SDK core imposes no Vault templates. Optional profile templates live under `plugins/<name>/templates/` and are installed only by `kg init --profile NAME` or `kg plugin install NAME`.
